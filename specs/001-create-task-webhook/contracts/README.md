# Contracts: Create-task webhook

This feature calls an external webhook defined by `V2A_CREATE_TODO_WEBHOOK_URL`.

## Files

- `create-todo-webhook.schema.json` — JSON Schema for the outbound webhook payload.
